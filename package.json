{
  "name": "student-management-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx json-server db.json --port $PORT"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
